import {PrintedInstructionKMMode} from "./printedInstructionKMMode";
import {Group} from "../keyMenu";

export class PrintedInstructionKMModeGroupGenerator<T> {
  generateGroup(mode: PrintedInstructionKMMode<T>): Group {
    //todo
    return new Group({});
  }
}
