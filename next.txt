
In general, you can return to parent modes from "submodes" by pressing escape,"ctrl-[", "shift-shift".  An exception is caps lock.  If caps lock is on, when going from an edit mode to a normal mode, you should go to "capslock normal mode".  Add this to the design notes.  Also, represent this in the mode label (use a "/" to separate parent modes from child modes). [[completed — added capslock normal/edit modes, "/" separator in mode labels, updated design_notes.md]]

Want support for undirected/bidirectional edges, or colors/line styles.  Make a submode.  Include shape in there as well. Replace the w submenu with access to this submode. have a submenu item that sets what an individual edge is and a menu item for setting the default.  this should also be how shapes work.  group shapes and directedness under a submenu.  Also give some options for colored nodes/edges and dotted/dashed/continuous edges. [[completed — style submenu (w key) with sub-submenus for shape, directedness, line style, color, and defaults. Commands handled in drawing-area.]]

need a representation of the timeing mechanism for shift-shift in edit mode.  Have a little bar on the shift key that moves down until shift-shift won't escape. [[completed — shift timing bar animates downward on first Shift press, clears on double-shift or timeout]]

snap-to grid.  implement this.  Make it visible when moving the crosshairs.  also make the pins and the waypoints visible when moving the crosshairs.  after moving, fade these things after 5 seconds. [[completed — crosshairs snap to grid, grid/pins/waypoints shown during movement, fade after 5s]]


Make labels editable.  Have them be attached to edges, and allow sliding them along the edges. [[completed — labels editable in labelEdit mode, dragging slides labels along their parent edge]]

when navigating the graph (g submenu), have an option to temporarily move all connected nodes close to the current node, so that they are visible. [[completed — gather (h key) animates connected nodes close, restores after 5s or on next gather]]

when navigating by graph, automatically select one of the outgoing edge (or incoming edge if you previously moved via an incoming edge) [[completed — auto-selects outgoing edge after graph navigation]]

for navigating by graph submenu, get rid of h and l, and make j and k be forwards (through the currently selected edge) and backwards (through the previously navigated through edge). [[completed — removed h/l movement keys from graph submenu, j=forwards k=backwards via selected edge, added navigation history]]
