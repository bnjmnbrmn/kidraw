I think I want there to be some sort of quick settings panel, orthogonal to the varios modes in the key menu.  Basically, I think it is probably easier to just switch to, say, a "move by node" or "graph move", instead of having to hold down a key like t.  Although that (the temporary movement mode switch via holding down t or g or whatever) may still be useful.  This also applies to edge directedness, node shape, color, edge style, font, etc.  This is something that could be addressed by having a bunch more roots and submenus, but I think we would run into exponential explosion of these menus

The style submenu (w) doesn't seem to be working (or maybe I'm not using it properly).

Make the shift-shift timing mechanism slower, at least for now.  Say 3 seconds.

The lines in the snap-to grid are too hard to see, especially when zoomed out.  Maybe make the grid spacing be dependent on the zoom level, too.

Labels don't seem to be selectable.  They may not be editable either.  Also, I don't want them to have visible boxes, by default.

We are going to need to work together on this "gather" feature. Probably needs to be recursive, somewhat at least, and to push away some nodes.  We'll want to look into applying layouts more generally.

"next edge out" doesn't seem to be working.  We'll need to debug this.

